---
name: notion_daily_reporter
description: Git 이력과 Brain 기록을 분석하여 상세 R&D 리포트를 작성하고 Notion에 자동 업로드하는 스킬
---

# Notion Daily Reporter

이 스킬은 사용자의 하루 작업 내용을 "상세하고 읽기 쉬운 R&D 리포트"로 정리하여 Notion에 업로드합니다.
**주의**: 반드시 `WebPilot-Engine` 프로젝트의 내용만 다뤄야 합니다.

## 1. 사전 점검 (Pre-flight Check)

- [ ] 현재 작업 디렉토리가 `WebPilot-Engine` 관련 경로인지 확인하십시오.
- [ ] `git log` 명령어를 사용할 수 있는지 확인하십시오.

## 2. 데이터 수집 (Data Collection)

- 오늘 날짜(또는 사용자가 지정한 날짜)의 Git 변경 사항을 수집합니다.

  ```bash
  git log --since="00:00" --pretty=format:"%h - %s" 
  ```

- Brain의 `task.md`를 읽어 완료된 작업(`[x]`)을 확인합니다.

## 3. 리포트 작성 (Report Generation)

수집된 데이터를 바탕으로 Markdown 리포트를 작성합니다.

- **파일명**: `daily_rnd_report_YYYY_MM_DD.md` (Artifacts 디렉토리)
- **형식**:

  ```markdown
  # [R&D] {주요 작업 요약} (YYYY-MM-DD)
  ## 1. 개요 (Overview)
  ## 2. 상세 작업 (Details) - Why & How 포함
  ## 3. 트러블슈팅 (Issues)
  ## 4. 인사이트 (Insights)
  ```

- **작성 원칙**:
  - 단순 나열 지양, **맥락(Context)** 위주 작성.
  - 다른 프로젝트의 내용이 섞이지 않도록 주의 (필터링).

## 4. Notion 업로드 (Upload to Notion)

작성된 리포트를 Notion 페이지에 업로드합니다.

- **Target URL**: `https://www.notion.so/etribe/yesol-heo-2e9cdc7c24e3800cbc87fe194f20b6ac`
- **Actions**:
  1. Browser Subagent 실행.
  2. 위 URL로 접속 (로그인 상태 확인).
  3. 'R&D 기록' 데이터베이스에서 **[+ 새로 만들기]** 버튼 클릭.
  4. **제목**: `[R&D] {주요 작업 요약} (YYYY-MM-DD)`
  5. **날짜**: 해당 날짜 선택.
  6. **본문**: 작성한 Markdown 내용 붙여넣기.
  7. **저장**: 별도 저장 버튼 없으므로 입력 후 잠시 대기.

## 5. Smart Notification (Slack)

Notion 업로드가 완료되면 Slack 웹훅을 통해 알림을 전송합니다.

- **Payload**:

  ```json
  {
    "text": "📅 *Daily R&D Report Uploaded* (YYYY-MM-DD)\n> *Topic*: {Summary}\n> *Link*: {Notion_Page_URL}"
  }
  ```

## 6. 실행 예시

이 스킬은 보통 워크플로우(`/report`)를 통해 호출됩니다.
