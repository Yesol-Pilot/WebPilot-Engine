---
description: 작업 시작 전 반드시 수행해야 하는 필수 점검 프로토콜 (Pre-flight Check)
---

이 워크플로우는 에이전트가 `Context Blindness`에 빠지지 않고 사용자의 **Master Rules**를 준수하기 위해 **모든 코딩/테스트 작업 직전**에 반드시 실행해야 한다.

# 0. 마스터 바이블 정독 (Holy Scripture)

`C:\Users\CTS_Sol\.gemini\antigravity\brain\41936156-d49f-47ed-9adf-9e54c2f1fda9\master_bible.md` 파일을 열고, 이번 작업이 위배하는 원칙이 없는지 확인한다.

# 1. 실행 환경 정찰 (Environment Recon)

가정을 버리고 실제 팩트를 수집한다.

1. `ADDITIONAL_METADATA`의 `Running terminal commands` 섹션을 **한 글자도 빼지 않고** 읽는다.
2. 만약 정보가 부족하다면, **절대** 3000번 포트라고 단정 짓지 말고 사용자에게 물어보거나 터미널 로그를 확인한다.
3. **확인된 포트**를 기록한다. (예: 3002)

# 2. 마스터 룰 준수 체크 (Rule Compliance Check)

다음 체크리스트를 하나라도 만족하지 못하면 작업을 시작할 수 없다.

- [ ] **언어**: 사고(Thought), 계획, 주석, 메시지 등 **모든 텍스트가 100% 한국어**인가?
- [ ] **포트**: 테스트 계획(Plan)에 **현재 실행 중인 포트**가 명시되었는가? (예: `localhost:3002`)
- [ ] **순서**: `Plan` -> `Files` -> `Code` -> `Test` 순서를 따르고 있는가?
- [ ] **테스트**: 브라우저 테스트 시 "무엇을 검증할지" 한글로 명확히 정의되었는가?

# 3. 실행 (Execute)

위 1, 2번이 완료되면 그제서야 `task_boundary`를 설정하거나 코드를 작성한다.

> **경고**: 이 절차를 건너뛰고 바로 코드를 작성하거나 테스트를 돌리면 **작업은 실패한 것으로 간주한다.**
